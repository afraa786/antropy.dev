# client

Frontend for Entropy (Next.js dashboard). Not yet implemented — this directory
is a placeholder so the intended project layout is visible in the repo.

Planned: the dashboard that consumes the backend API in `../server` (scan
results, findings, AI summaries).
