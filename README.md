1. Entropy has a fully working FastAPI backend running live against hosted Supabase Postgres (with Redis + a Celery worker) that covers the complete user journey register, login (JWT with refresh + Redis token revocation), create organizations/projects, register domains, and prove domain ownership via DNS-TXT or HTTP-file verification all gated so you can never scan a domain you haven't verified.

2. A new one-call quick-scan endpoint hides that whole sequence (auto-creating workspace, project, and domain, with a demo-only verification bypass) so the frontend just sends a target and gets a scan running.

3. On the scanning side, the orchestrator now actually produces real findings: a stdlib SSL/TLS engine (expired/expiring certs, hostname mismatch, self-signed, weak ciphers, deprecated protocols) runs inline, and a urlscan.io engine (malicious verdict, missing security headers, tech stack, TLS, screenshot, redirects) runs as a progressive background task that appends its results after the fast engines finish all normalized into one schema, persisted, and annotated with an AI-generated summary (OpenRouter, with a graceful fallback when rate-limited).
   
4. The gaps are still the frontend dashboard and VS Code extension (empty folders), plus the other engines (Nuclei/Katana/TruffleHog) that remain unimplemented but the backend spine, two live scanner engines, AI summaries, and the one-call demo flow are all real and tested (21 passing tests).
