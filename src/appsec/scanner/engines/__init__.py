# Importing this package will (once adapters exist) trigger registration of
# every engine via `@register_scanner`. Currently empty — no adapters exist
# yet, only READMEs describing each engine's expected responsibility.
