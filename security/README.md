# security

Security tooling / scanner assets for Entropy. Not yet populated — this
directory is a placeholder so the intended project layout is visible in the repo.

The active scanner engines currently live in
`../server/src/appsec/scanner/engines` (e.g. `ssl_tls`, `urlscan`).
